local downloadsDirectory = os.getenv("HOME") .. "/Downloads/"

-- function handleDownloadsChange(files)
--   hs.task.new('/usr/local/bin/tag')
--   for _, file in pairs(files) do
--   end
-- end

-- downloadsWatcher = hs.pathwatcher.new(downloadsDirectory, ):start()
